# Sprint 5 作業指示書 — AI実装

## 1. 概要

Sprint 5 のゴールは **「戦術 AI と戦略 AI が動き、プレイヤーが何もしなくても他国同士で戦争が起きる」** ことです。
**本スプリントは Sprint 1〜6 のなかで最も難所**です。スコープを厳格に守り、欲を出さないこと。

> ⚠️ AI の挙動は実装してみないと分からない部分が多いため、**このドキュメントは"出発点"**です。
> 実装中にバランスや挙動の問題が見つかれば、Web 版 Claude と相談して仕様を調整してください。

---

## 2. 完了条件

### 戦術 AI（戦闘画面）

- [ ] 防衛側 AI ターンで全ユニットが自動的に行動する
- [ ] AI ユニットは攻撃範囲内に敵がいれば攻撃する
- [ ] AI ユニットは攻撃範囲外なら最寄りの敵に向かって移動する
- [ ] AI 行動はターン内で1ユニットずつ順次実行される（一瞬で全部進まない）
- [ ] AI が全ユニット行動完了したら自動的にターンが切り替わる

### 戦略 AI（国取り画面）

- [ ] AI 国家ターンで自動的に侵攻判断が行われる
- [ ] AI は最も弱い隣接敵領地を攻める傾向を持つ
- [ ] 自国が劣勢（兵力1名以下）なら侵攻を控える
- [ ] AI 同士の戦闘は自動解決される（戦術マップを表示しない）
- [ ] プレイヤーが関与しない戦闘でも領地遷移が起きる

### バランス

- [ ] AI が完全に賢すぎず、プレイヤーが勝てる
- [ ] AI 同士の戦争で勢力図が変動する（プレイヤーを放置すると AI 同士で潰し合う）
- [ ] 1局 30分〜1時間で決着がつく

---

## 3. スコープ

### 含む

- 戦術 AI（移動 + 攻撃判断）
- 戦略 AI（侵攻 / パス判断）
- AI 同士の戦闘自動解決
- AI 行動の視覚的フィードバック（順次実行）

### 含まない

- ❌ 高度な AI（連携プレイ・誘い込み・釣り出し）
- ❌ 性格別 AI（攻撃的/守備的など国ごとの個性）— Sprint 7 以降
- ❌ プレイヤーの行動を学習する AI
- ❌ 同盟・外交（依然 MVP 対象外）
- ❌ AI の難易度設定 UI

---

## 4. 事前決定事項

### 戦術 AI のアルゴリズム

**「最寄りの敵を狙う」だけ**の超シンプル AI。

```
各 AI ユニットについて:
  1. 攻撃可能な敵がいれば、最も HP が低い敵を攻撃 → 終了
  2. 攻撃可能な敵がいなければ:
     a. 全敵ユニットへの距離を計算（マンハッタン）
     b. 最寄りの敵に向かって、移動可能範囲内で最も近づけるマスへ移動
     c. 移動後、再度攻撃可能チェック
        - 攻撃可能なら攻撃
        - 不可なら待機
  3. 移動も攻撃もできない（囲まれているなど）場合は待機
```

**やらないこと（Sprint 5 では実装しない）**:
- 弱い敵を集中攻撃する戦略
- 防御陣形を組む
- 弓兵を後方に置く
- 撤退判断

### 戦略 AI のアルゴリズム

**「弱い隣国を狙う + 自分が弱ければパス」**だけのシンプル AI。

```
AI 国家ターンで、各所有領地について:
  1. その領地の駐留兵が0なら侵攻不可、スキップ
  2. 隣接する敵領地のリストを取得
  3. 隣接敵領地の中から、駐留兵数が最も少ない領地を選ぶ
  4. 自国の駐留兵が 1名以下なら侵攻しない（防衛優先）
  5. 兵数 ≥ 2 なら、選んだ敵領地に侵攻する
  6. 各領地は1ターンに1回まで侵攻

優先順位:
  - 兵数差が大きい（楽に勝てる）領地ほど優先
  - 同条件なら何かしらの順序で決定的に選ぶ（例：territory.id の辞書順）
```

**やらないこと**:
- プレイヤーへの集中攻撃
- 連携プレイ
- 経済戦略（金を貯める判断）

### AI 同士の自動戦闘

戦術マップを開かずに、Sprint 2 のダミー戦闘ロジックを使って即決:

```
function resolveAIBattle(attacker: Nation, defender: Nation, ...) {
  // Sprint 2 で作った「兵数比較ベース」のロジックを使う
  // または: HPと ATK 合計値で簡易シミュレーション
}
```

**正確な戦闘シミュレーションは行わない**（パフォーマンス問題と、プレイヤーが見えない部分なので雑でOK）。

ただし**プレイヤーが攻撃側 or 防衛側の場合は戦術マップを開く**。

### AI 実行の演出

```
戦術 AI:
  - 1ユニット行動につき 0.5〜1秒のディレイ
  - 移動 → 短い停止 → 攻撃 の順に視覚的に分かるよう

戦略 AI:
  - 1国家の処理につき 0.5秒程度のディレイ
  - 「ノルダー氏族が侵攻します...」程度のメッセージ表示
```

長すぎると待ちがストレスになる。早すぎると何が起きたか分からない。

---

## 5. 状態の拡張

### `src/game/types.ts`

特に大きな追加は不要。既存の状態で AI ロジックは書ける。

### 必要に応じて追加

```ts
export interface GameState {
  // 既存
  // 追加候補
  aiActionLog: string[];     // AI行動の履歴（直近10件）
  isAIThinking: boolean;     // AI処理中フラグ（UI抑制用）
}
```

---

## 6. 新規ファイル

```
src/
  game/
    tactical/
      ai.ts                ← 戦術 AI
    strategic/
      ai.ts                ← 戦略 AI
      autoBattle.ts        ← AI 同士戦闘の自動解決
```

---

## 7. 実装手順（推奨順序）

### Phase 1: 戦術 AI

1. **戦術 AI の純粋関数化**
   - `tactical/ai.ts` に `decideAIUnitAction(unit, state)` を実装
   - 入力: ユニットと戦況、出力: `{ type: 'move'|'attack'|'wait', ... }`
   - 純粋関数として書き、テストしやすくする

2. **AI ターンの実行フロー**
   - 各 AI ユニットを順次処理
   - 1アクションごとに setTimeout で遅延
   - 全ユニット終了後にターン切替

3. **動作確認**
   - 戦闘画面で AI が動くこと
   - プレイヤーが何もしないと AI に倒されること

### Phase 2: 戦略 AI

4. **戦略 AI の純粋関数化**
   - `strategic/ai.ts` に `decideAINationActions(nation, state)` を実装
   - 各領地の侵攻判断を返す

5. **AI 国家ターンの実装**
   - Sprint 2 で「即パス」だった部分を本実装に置換
   - プレイヤーが関与する戦闘は戦術マップ起動
   - そうでなければ自動解決

6. **演出**
   - AI 国家の行動メッセージ表示
   - ディレイ調整

### Phase 3: 統合とバランス

7. **長時間プレイテスト**
   - プレイヤーが何もしない放置プレイで AI 同士の戦争が成立するか確認
   - 1局完走が現実的な時間で終わるか

8. **バランス調整**
   - AI が強すぎたら戦術 AI の判断を弱体化（例: HP 低い敵を狙う → ランダムターゲット）
   - AI が弱すぎたら攻撃優先順位を強化

---

## 8. テストシナリオ

### シナリオ A — 戦術 AI

1. プレイヤーから侵攻して戦闘画面に入る
2. 攻撃せずにターン終了
3. ✅ 防衛側 AI ユニットが順次行動する
4. ✅ AI ユニットがプレイヤーに近づき、隣接したら攻撃してくる
5. ✅ プレイヤーが受けるダメージが計算通り

### シナリオ B — 戦略 AI

1. プレイヤーが「ターン終了」を押す
2. ✅ 各 AI 国家が順次行動する
3. ✅ 行動メッセージが表示される
4. ✅ AI 同士の戦闘で領地遷移が起きる
5. ✅ 1ターンで全 AI 国家の処理が終わる

### シナリオ C — 放置プレイ

1. プレイヤーは中央領地で動かず、ターン終了を繰り返す
2. ✅ AI 同士の戦争で勢力図が変動する
3. ✅ 数十ターン経過で 1〜2カ国が滅亡する
4. ✅ ゲームが進行不能にならない（無限ループ等が起きない）

### シナリオ D — AI vs AI 自動戦闘

1. 戦闘に関与しないターンを進める
2. ✅ 戦術マップが開かない
3. ✅ 戦略マップ上で領地が移り変わる
4. ✅ ログに「○○が△△を占領」など表示

### シナリオ E — プレイヤー vs AI 通常戦

1. AI 国家がプレイヤー領地に侵攻してくる
2. ✅ プレイヤーが防衛側の戦術マップが起動
3. ✅ 攻撃側が AI として動く
4. ✅ プレイヤーが防衛側ユニットを操作できる

---

## 9. リスク管理

このスプリントで発生しがちな問題と対処:

| 問題 | 対処 |
|---|---|
| AI が無限に同じ動きを繰り返す | 30ターン上限で強制決着 |
| AI が動かない・ハングする | console.log で各判断段階を出力、デバッグ |
| AI が強すぎてプレイヤーが詰む | 戦術 AI のターゲット選択を弱体化（最弱→ランダム） |
| AI が弱すぎて緊張感ない | 戦略 AI に「兵数2以上で必ず侵攻」のルールを足す |
| AI 処理が重くてカクつく | setTimeout のディレイ調整、計算の最適化 |

**詰まったら一度 Web 版 Claude と相談すること**。AI の挙動は実装してみないと分からない部分が大きい。

---

## 10. 補足

- **戦術 AI と戦略 AI は完全に別ファイル**（混ぜない）
- **AI ロジックは純粋関数として書く**（state を引数で受けて、アクションを返すだけ）
- **state を直接書き換えない**（Zustand のアクション経由でのみ更新）
- **テストしやすさ最優先**（後で挙動を変えやすくするため）

不明点・挙動の相談は Web 版 Claude へ。
