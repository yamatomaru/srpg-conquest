# Sprint 3 作業指示書 — 戦術マップ実装（移動フェーズ）

## 1. 概要

Sprint 3 のゴールは **「戦略マップの侵攻ボタンから戦術マップに遷移し、グリッド上でユニットを移動させて戦略マップに戻れる」** ことです。
攻撃・ダメージ計算は Sprint 4 で実装するため、本スプリントでは **「移動と決着判定の枠組み」だけ** を作ります。

> ⚠️ 着手前に、Sprint 2 完了時の実装を踏まえて本指示書を Web 版 Claude と一緒にレビューしてください。
> 特に Zustand のアクション粒度と SVG コンポーネントの分割方針は、Sprint 2 の実装に合わせて調整が必要です。

---

## 2. 完了条件

### 画面遷移

- [ ] 戦略マップで侵攻を実行すると、戦術マップ画面に切り替わる
- [ ] 戦術マップ終了後、戦略マップに戻る
- [ ] 戻った時点で領地の所有権・駐留兵が正しく更新されている

### 戦術マップ表示

- [ ] 8×8 のグリッドが表示される
- [ ] 攻撃側ユニットが左半分、防衛側ユニットが右半分に初期配置される
- [ ] 各ユニットはジョブを示す円（または記号）で表示される（スプライトは未着手でOK）
- [ ] ユニットの上に HP / ジョブ略号が表示される
- [ ] 攻撃側は青、防衛側は赤の枠で識別できる

### ユニット操作

- [ ] ユニットをクリックすると選択状態になる
- [ ] 選択中のユニットの移動可能範囲がハイライトされる（BFS）
- [ ] ハイライト範囲内のマスをクリックするとユニットが移動する
- [ ] 移動済みユニットは半透明になり、再選択しても移動範囲が出ない
- [ ] ユニット選択中に空のマスをクリックしたら選択解除

### ターン進行

- [ ] 「ターン終了」ボタンで攻撃側ターン → 防衛側ターン → 攻撃側ターンの順で進む
- [ ] ターン開始時に該当陣営の `hasMoved` が全リセットされる
- [ ] 防衛側 AI ターンは Sprint 3 ではパス（即終了）でOK

### 決着（仮）

- [ ] 「降参する」ボタンで強制的に防衛側勝利として戦略マップに戻る
- [ ] 「占領する」ボタンで強制的に攻撃側勝利として戦略マップに戻る
- [ ] **実際の戦闘決着判定は Sprint 4 で実装**

---

## 3. スコープ

### 含む

- 戦術マップの SVG グリッド描画
- ユニット初期配置ロジック
- BFS による移動範囲計算
- ユニット選択 → 移動範囲ハイライト → 移動の操作フロー
- ターン制（攻撃側 ⇄ 防衛側）
- 戦略マップへの戻り処理
- 戦略マップ ⇄ 戦術マップ間のデータ受け渡し

### 含まない

- ❌ 攻撃アクション（ダメージ計算・撃破処理）— Sprint 4
- ❌ 戦術 AI（防衛側 AI は即パス）— Sprint 5
- ❌ 地形効果（全マス平地）
- ❌ 必殺技・属性
- ❌ 戦闘アニメーション
- ❌ スプライト画像（円や記号で代用）

---

## 4. 事前決定事項

### 戦術マップサイズ

- グリッド: **8×8**（拡張余地あり）
- マスサイズ: 60px 角
- マップ全体サイズ: 480px × 480px
- 余白を含めて SVG は 600px × 600px 程度

### 初期配置ルール

```
攻撃側: 左の3列（x=0,1,2）に縦並びで配置
防衛側: 右の3列（x=5,6,7）に縦並びで配置

戦士・弓兵・魔導師の混在パーティでも、特に配置順は問わない。
人数が3を超えたら最前列から詰めて配置（最大4列まで対応）。
```

### 移動範囲計算

- BFS（幅優先探索）で `mov` マス以内の到達可能セルを列挙
- 移動コストは全マス1（地形は Sprint 7 以降）
- **他ユニットがいるマスは通過不可**
- 自分自身のマスは到達範囲に含めない（移動キャンセル＝そのままターン保持）

### ターン進行ルール

```
1. 戦闘開始時、攻撃側ターンから始まる
2. プレイヤーが「ターン終了」を押すと相手側のターン
3. 防衛側 AI は Sprint 3 では即「ターン終了」と同等
4. ターン切替時に新しい行動側全ユニットの hasMoved = false
5. 30 ターン経過したら強制的に防衛側勝利（無限ループ防止、Sprint 5 で見直し）
```

### 戻り処理

- **攻撃側勝利時**: Sprint 2 のダミー戦闘と同じ処理（領地占領、半数移動）
- **防衛側勝利時**: 攻撃側ユニットを攻撃側 Nation から削除、防衛側そのまま
- **戦闘解決ロジックは `src/game/battle.ts` に集約**（Sprint 2 で作ったものを拡張）

---

## 5. 状態の拡張

### `src/game/types.ts`

Sprint 1 で型定義した `BattleState` / `BattleUnit` をそのまま使えるはず。
不足があれば以下のような追加を検討：

```ts
export interface BattleState {
  // 既存フィールド
  attackerNationId: string;
  defenderNationId: string;
  territoryId: string;
  map: TacticalMap;
  units: BattleUnit[];
  currentSide: Side;
  turnCount: number;

  // 追加候補
  selectedUnitId: string | null;
  reachableCells: { x: number; y: number }[]; // 選択中ユニットの移動範囲
  maxTurns: number;                            // デフォルト30
}
```

### `src/game/store.ts` のアクション

```ts
interface GameActions {
  // 既存...

  // 戦闘開始・終了
  startBattle: (fromTerritoryId: string, toTerritoryId: string) => void;
  endBattle: (winnerSide: Side) => void;

  // 戦術マップ操作
  selectUnit: (unitId: string | null) => void;
  moveUnit: (unitId: string, to: { x: number; y: number }) => void;
  endTacticalTurn: () => void;
}
```

---

## 6. 新規ファイル

```
src/
  components/
    TacticalMap.tsx        ← グリッド・ユニット描画・クリック処理
    TacticalControl.tsx    ← ターン情報・ターン終了/降参ボタン
    UnitInfo.tsx           ← 選択中ユニットの詳細表示（オプション）
  game/
    tactical/
      movement.ts          ← BFS による移動範囲計算
      placement.ts         ← 初期配置ロジック
```

`battle.ts` は既存のものを拡張する形で OK。

---

## 7. 実装手順（推奨順序）

1. **戦術マップへの遷移を骨組みだけ作る**
   - `executeInvasion` を改修して戦闘画面に切り替えるだけ
   - 戦術マップは「降参/占領ボタンだけがある画面」でOK
   - 戻り処理を Sprint 2 のロジック流用で動かす

2. **静的なグリッドとユニット表示**
   - 8×8 グリッドを描画
   - 初期配置ロジック (`placement.ts`) でユニットを配置
   - クリック反応はまだ実装しない

3. **ユニット選択と移動範囲ハイライト**
   - `selectUnit` 実装
   - `movement.ts` の BFS を実装
   - 選択中ユニットの到達セルをハイライト

4. **移動の実行**
   - ハイライトセルクリックで `moveUnit`
   - `hasMoved` フラグで半透明化

5. **ターン制**
   - 「ターン終了」ボタン
   - 防衛側 AI は即パス（タイマーで0.5秒待ってからターン戻す程度の演出はオプション）

6. **30ターン上限と戻り処理の整備**
   - 強制決着判定
   - 戦略マップへの遷移と所有権更新

---

## 8. テストシナリオ

### シナリオ A — 戦闘画面遷移

1. ゲーム開始 → 中央領地から西へ侵攻
2. ✅ 戦術マップ画面が表示される
3. ✅ 攻撃側3名（中央駐留）が左に、防衛側2名（西駐留）が右に配置される
4. 「占領する」ボタンを押す
5. ✅ 戦略マップに戻る
6. ✅ 西の所有者がアルバニアに変わっている

### シナリオ B — 移動操作

1. 戦闘画面で攻撃側ユニットをクリック
2. ✅ 移動可能範囲がハイライト
3. ハイライト内のセルをクリック
4. ✅ ユニットが移動する
5. ✅ ユニットが半透明になる
6. ✅ 同じユニットを再クリックしても範囲が出ない

### シナリオ C — ターン制

1. 全攻撃側ユニットを移動させる
2. 「ターン終了」ボタンを押す
3. ✅ 防衛側ターンになる（AI 即パス）
4. ✅ 攻撃側ターンに戻る
5. ✅ 全攻撃側ユニットが再び移動可能になる

---

## 9. 補足

- **BFS実装はSprint 5の戦術AIでも使う**ので、純粋関数として切り出すこと
- **30ターン上限は仮**。Sprint 5 で AI が攻撃するようになったら見直す
- **戦闘画面のレイアウト**は Sprint 6 で整備するので、Sprint 3 では機能優先で雑でOK

不明点は実装前に Web 版 Claude へ。
